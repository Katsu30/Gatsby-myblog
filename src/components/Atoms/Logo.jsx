import React from 'react';
import styled from 'styled-components';

const Logo = ( ...props ) => {
  return (
    <LogoTextWithStyled>
      K's Blog
    </LogoTextWithStyled>
  )
}

const LogoTextWithStyled = styled.div`
`;

export default Logo;